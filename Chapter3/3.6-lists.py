# 3.6 lists

# accessing an element by index is constant time
# O(1) - constant time: same amount of time regardless of the list size
# O(N) means in proportion to the number of items
# using append() is also O(1) on average, but it can be O(n) in the worst case if the list needs to be resized.